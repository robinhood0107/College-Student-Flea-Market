<!DOCTYPE html>
<html class="light" lang="ko">
<head>
  <%- include('../partials/head') %>
  <title>관리자 대시보드 | Campus flea Market</title>
</head>
<body class="font-display bg-main text-slate-800 dark:text-slate-200">
  <div class="relative flex h-auto min-h-screen w-full flex-col group/design-root overflow-x-hidden">
    <div class="layout-container flex h-full grow flex-col">
      <%- include('../partials/header') %>
      
      <!-- 메인 콘텐츠 -->
      <main class="flex-1 px-4 sm:px-6 lg:px-10 py-8">
        <div class="max-w-7xl mx-auto flex flex-col gap-8">
          <!-- 페이지 제목 -->
          <div class="flex flex-wrap justify-between gap-3 items-center">
            <p class="text-slate-900 dark:text-white text-3xl md:text-4xl font-black leading-tight tracking-[-0.033em] min-w-72">관리자 대시보드</p>
          </div>
          <!-- 통계 카드 -->
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
            <div class="flex min-w-[158px] flex-1 flex-col gap-2 rounded-lg p-6 border border-main bg-white dark:bg-slate-900">
              <p class="text-slate-600 dark:text-slate-400 text-base font-medium leading-normal">총 회원 수</p>
              <p class="text-slate-900 dark:text-white tracking-light text-2xl font-bold leading-tight"><%= typeof stats !== 'undefined' ? stats.totalUsers.toLocaleString() : '0' %></p>
            </div>
            <div class="flex min-w-[158px] flex-1 flex-col gap-2 rounded-lg p-6 border border-main bg-white dark:bg-slate-900">
              <p class="text-slate-600 dark:text-slate-400 text-base font-medium leading-normal">총 상품 수</p>
              <p class="text-slate-900 dark:text-white tracking-light text-2xl font-bold leading-tight"><%= typeof stats !== 'undefined' ? stats.totalProducts.toLocaleString() : '0' %></p>
            </div>
            <div class="flex min-w-[158px] flex-1 flex-col gap-2 rounded-lg p-6 border border-main bg-white dark:bg-slate-900">
              <p class="text-slate-600 dark:text-slate-400 text-base font-medium leading-normal">오늘의 신규 가입자</p>
              <p class="text-slate-900 dark:text-white tracking-light text-2xl font-bold leading-tight"><%= typeof stats !== 'undefined' ? stats.todayUsers.toLocaleString() : '0' %></p>
            </div>
            <div class="flex min-w-[158px] flex-1 flex-col gap-2 rounded-lg p-6 border border-main bg-white dark:bg-slate-900">
              <p class="text-slate-600 dark:text-slate-400 text-base font-medium leading-normal">오늘의 신규 상품</p>
              <p class="text-slate-900 dark:text-white tracking-light text-2xl font-bold leading-tight"><%= typeof stats !== 'undefined' ? stats.todayProducts.toLocaleString() : '0' %></p>
            </div>
          </div>
          <!-- 관리 섹션 -->
          <div class="flex flex-col gap-8">
            <!-- 회원 관리 -->
            <div class="flex flex-col gap-4">
              <h2 class="text-slate-900 dark:text-white text-[22px] font-bold leading-tight tracking-[-0.015em]">회원 관리</h2>
              <div class="w-full overflow-hidden rounded-lg border border-main bg-white dark:bg-slate-900">
                <div class="overflow-x-auto">
                  <table class="w-full text-left">
                    <thead>
                      <tr class="bg-slate-50 dark:bg-slate-800/50">
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">ID</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">이메일</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">닉네임</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">가입일</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal text-right">관리</th>
                      </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-200 dark:divide-slate-800">
                      <% if (typeof users !== 'undefined' && users.length > 0) { %>
                        <% users.forEach(user => { %>
                        <tr>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= user.id %></td>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= user.email %></td>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= user.name %></td>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= user.created_at ? new Date(user.created_at).toLocaleDateString('ko-KR') : '-' %></td>
                          <td class="h-[72px] px-4 py-2 text-right">
                            <form action="/admin/user/<%= user.id %>/delete" method="POST" style="display: inline;">
                              <button type="submit" class="px-3 py-1.5 text-sm font-semibold text-red-600 dark:text-red-400 bg-red-100 dark:bg-red-900/40 rounded hover:bg-red-200 dark:hover:bg-red-900/60">강제 탈퇴</button>
                            </form>
                          </td>
                        </tr>
                        <% }); %>
                      <% } else { %>
                        <tr>
                          <td colspan="5" class="px-4 py-8 text-center text-sub">등록된 회원이 없습니다.</td>
                        </tr>
                      <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
            <!-- 상품 관리 -->
            <div class="flex flex-col gap-4">
              <h2 class="text-slate-900 dark:text-white text-[22px] font-bold leading-tight tracking-[-0.015em]">상품 관리</h2>
              <div class="w-full overflow-hidden rounded-lg border border-main bg-white dark:bg-slate-900">
                <div class="overflow-x-auto">
                  <table class="w-full text-left">
                    <thead>
                      <tr class="bg-slate-50 dark:bg-slate-800/50">
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">상품명</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">판매자</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">가격</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal">등록일</th>
                        <th class="px-4 py-3 text-slate-700 dark:text-slate-300 text-sm font-medium leading-normal text-right">관리</th>
                      </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-200 dark:divide-slate-800">
                      <% if (typeof adminProducts !== 'undefined' && adminProducts.length > 0) { %>
                        <% adminProducts.forEach(product => { %>
                        <tr>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= product.title %></td>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= product.seller ? product.seller.name : '알 수 없음' %></td>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap">₩<%= product.price.toLocaleString() %></td>
                          <td class="h-[72px] px-4 py-2 text-sub text-sm font-normal leading-normal whitespace-nowrap"><%= product.created_at ? new Date(product.created_at).toLocaleDateString('ko-KR') : '-' %></td>
                          <td class="h-[72px] px-4 py-2 text-right">
                            <form action="/admin/product/<%= product.id %>/delete" method="POST" style="display: inline;">
                              <button type="submit" class="px-3 py-1.5 text-sm font-semibold text-red-600 dark:text-red-400 bg-red-100 dark:bg-red-900/40 rounded hover:bg-red-200 dark:hover:bg-red-900/60">삭제</button>
                            </form>
                          </td>
                        </tr>
                        <% }); %>
                      <% } else { %>
                        <tr>
                          <td colspan="5" class="px-4 py-8 text-center text-sub">등록된 상품이 없습니다.</td>
                        </tr>
                      <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>
</body>
</html>

